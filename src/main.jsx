import React from "react";
import ReactDOM from "react-dom/client";
import KingdomScheduler from "./KingdomScheduler.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <KingdomScheduler />
  </React.StrictMode>
);
